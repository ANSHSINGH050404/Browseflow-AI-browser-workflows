name: Deploy Trigger

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-trigger-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy Trigger.dev workers
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Deploy tasks
        env:
          # Prefer TRIGGER_ACCESS_TOKEN (Trigger.dev → Account → Access Tokens).
          # Falls back to TRIGGER_SECRET_KEY if you only set that secret.
          TRIGGER_ACCESS_TOKEN: ${{ secrets.TRIGGER_ACCESS_TOKEN || secrets.TRIGGER_SECRET_KEY }}
        run: npx trigger.dev@4.5.3 deploy --env prod
